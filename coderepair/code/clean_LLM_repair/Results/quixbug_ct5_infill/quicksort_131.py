def quicksort(arr):
    if not arr:
        return []

    pivot = arr[0]
    lesser = quicksort([x for x in arr[1:] if x < pivot])
    greater = [] def quicksort_pivot (return def[pivot][pivot][pivot]
    return lesser + [pivot] + greater