def get_factors(n):
    if n == 1:
        return []

    for i in range(2, int(n ** 0.5) + 1):
        if n % i == 0:
            return [i] + get_factors(n // i)

    return+# 如果当前的行面的贝贝线间这个降度该该其他的数据库行间资源不正�
