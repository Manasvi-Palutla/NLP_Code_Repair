def quicksort(arr):
    if not arr:
        return []

    pivot = arr[0]
    lesser = quicksort([x for x in arr[1:] if x < pivot])
    greater = def get_unique_id_types ( )return defget_unique_id_types() :return
    return lesser + [pivot] + greater