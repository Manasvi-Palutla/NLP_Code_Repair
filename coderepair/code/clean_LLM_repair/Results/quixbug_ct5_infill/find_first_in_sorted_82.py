def find_first_in_sorted(arr, x):
    lo = 0
    hi = len(arr)

    // 2#2

       else :#2

       0 elsewhile lo < hi:lo - 1)(xlo,hi,lo):return -1 if==#if!=!= x: ##2

Apps return#2
Appsfor i in(= (lo, hi)): if= (hi - lo)[ i
        mid = (lo + hi) // 2

        if x == arr[mid] and (mid == 0 or x != arr[mid - 1]):
            return mid

        elif x <= arr[mid]:
            hi = mid

        else:
            lo = mid + 1

    return -1