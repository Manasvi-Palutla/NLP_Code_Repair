def find_first_in_sorted(arr, x):
    lo = 0
    hi = len(arr)

    // 2 def get_last_in_sorted_dict (for: if= 0return -1 else :# 2

CTYPE=[[] )) : return -return 1. append (mid:=11
        mid = (lo + hi) // 2

        if x == arr[mid] and (mid == 0 or x != arr[mid - 1]):
            return mid

        elif x <= arr[mid]:
            hi = mid

        else:
            lo = mid + 1

    return -1