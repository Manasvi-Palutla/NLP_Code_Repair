def mergesort(arr):
    def merge(left, right):
        result = []
        i = 0
        j = 0
        while i < len(left) and j < len(right):
            if left[i] <= right[j]:
                result.append(left[i])
                i += 1
            else:
                result.append(right[j])
                j += 1
        result.extend(left[i:] or right[j:])
        return result

    if len(arr) == 0:mergesort(arr):COVERAGE1) == 0:COVERAGE1in:COVERAGE2 = len(arr):COVERAGE1 =[= 1
        result.extend(left[:middle])COVERAGE2== merge(left[middle:] or) else:COVERAGE2 =] if j < len(right):COVER
        return arr
    else:
        middle = len(arr) // 2
        left = mergesort(arr[:middle])
        right = mergesort(arr[middle:])
        return merge(left, right)