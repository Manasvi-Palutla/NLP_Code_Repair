def find_first_in_sorted(arr, x):
    lo = 0
    hi = len(arr)

    // 2else :else :+ 12


x < arr[lo]:
           hi = mid -] else :lo(lo, hi):= (lo+hi):
Terminology: #return lohi =+ 10

Container1 ,;hi ==] return mid

1=]0

Apps
        mid = (lo + hi) // 2

        if x == arr[mid] and (mid == 0 or x != arr[mid - 1]):
            return mid

        elif x <= arr[mid]:
            hi = mid

        else:
            lo = mid + 1

    return -1