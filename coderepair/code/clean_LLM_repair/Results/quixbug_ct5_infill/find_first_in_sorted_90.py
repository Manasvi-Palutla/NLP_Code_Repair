def find_first_in_sorted(arr, x):
    lo = 0
    hi = len(arr)

    // 2else :else : return -1if0:+ 1 :elif x >=loelse :( hi -else:else := 1


hi = lo - 1


xi in:=mideliflo1: return midlo = lo;= (
        mid = (lo + hi) // 2

        if x == arr[mid] and (mid == 0 or x != arr[mid - 1]):
            return mid

        elif x <= arr[mid]:
            hi = mid

        else:
            lo = mid + 1

    return -1