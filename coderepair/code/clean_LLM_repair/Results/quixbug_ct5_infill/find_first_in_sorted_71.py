def find_first_in_sorted(arr, x):
    lo = 0
    hi = len(arr)

    // 2else :else : return -1returnx >= arr[lo]:= 0

Auxiliary= 1

Auxiliaryhi ++ 1

Auxiliaryreturn -1

Auxiliary elseelse:hi = -1
Auxiliaryif x > arr[hi]:+=2
Auxiliary=hi +hi )return -1
        mid = (lo + hi) // 2

        if x == arr[mid] and (mid == 0 or x != arr[mid - 1]):
            return mid

        elif x <= arr[mid]:
            hi = mid

        else:
            lo = mid + 1

    return -1